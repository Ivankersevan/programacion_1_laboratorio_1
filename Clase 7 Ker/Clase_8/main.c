#include <stdio.h>
#include <stdlib.h>

typedef int persona;

struct S_Persona
{
    char nombre[70];
    int edad;
    char dni[20];
    float altura;
}

typedef struct S_Persona Persona;

int main()
{
persona.p;

p.edad=33;
p.altura=1.90;
p.nombre="Juan";
fgets(p.nombre,70,stdin);
struct S_Persona p;

int printPersona(int edad,int altura,char nombre);

return 0;
}

printPersona(p.edad,p.altura,p.nombre)
{
    printf("Edad: %d",p.edad);
    printf("Altura %d",p.altura);
    printf("Nombre %d",p.nombre);

}
